export default function Home() {
  return (
    <main
      style={{
        height: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "'Cinzel', serif",
        color: "#a8d8ff",
        letterSpacing: ".32em",
        textTransform: "uppercase",
        fontSize: 18,
      }}
    >
      CompanionCube
    </main>
  );
}
